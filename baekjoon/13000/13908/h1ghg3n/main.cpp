#include <iostream>
#include <vector>

using namespace std;

int main(){
    int m, n, c;
    cin >> m >> n;
    vector<int> password;
    for (int i = 0; i<n;i++){
        cin >> c;
        password.push_back(c);
    }
        
    return 0;
}