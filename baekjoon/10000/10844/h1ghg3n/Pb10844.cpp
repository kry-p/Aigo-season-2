#include <iostream>
#include <algorithm>

using namespace std;
int dp[101];

int main() {
	ios::sync_with_stdio(NULL);
	cin.tie(0);
	cout.tie(0);
	int n;
	cin >> n;
	dp[0] = 0;
	for (int i = 0; i <= n; i++) {
		//if 
	}

	return 0;
}